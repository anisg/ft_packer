char *insert_string(char *template, char *binaire_encrypter);

compress();

int main(){
	open(binaire);
	char *binaire_encrypter = compress(encrypt(binaire));
	insert_string(TEMPLATE, binaire_encrypter);
}
